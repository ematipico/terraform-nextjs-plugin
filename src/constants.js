const PROVIDERS = {
	AWS: "AWS"
};

const FILE_NAMES = {
	LAMBDAS: "lambdas.terraform.tf.json",
	GATEWAY: "gateway.terraform.tf.json"
};

const NEXT_CONFIG = "next.config.js";

module.exports = {
	PROVIDERS,
	FILE_NAMES,
	NEXT_CONFIG
};
